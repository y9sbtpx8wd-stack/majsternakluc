{
  "name": "majsternakluc",
  "private": true,
  "workspaces": [
    "apps/web",
    "apps/api"
  ],
  "scripts": {
    "dev:web": "yarn --cwd apps/web dev",
    "dev:api": "yarn --cwd apps/api start:dev",
    "build:web": "yarn --cwd apps/web build",
    "build:api": "yarn --cwd apps/api build",
    "start:web": "yarn --cwd apps/web start",
    "start:api": "yarn --cwd apps/api start:prod"
  },
  "prisma": {
  "schema": "apps/api/prisma/schema.prisma"
  }
}
